#!/usr/bin/env bash

cd "$(dirname "$BASH_SOURCE")"/..

source etc/env.sh

mkdir -p share

if [ "$NODE_ENV" = "production" ]; then
  browserify src/client.js | uglifyjs > share/build.js
else
  browserify --debug src/client.js > share/build.js
fi
